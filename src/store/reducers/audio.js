const initialState = {
  musicVAE: null,
  melodyLibrary: [],
};

function audioReducer(state = initialState, action) {
  switch (action.type) {
    // handle different actions
    default:
      return state;
  }
}

export default audioReducer;
